import type { AppRoute } from '../../../app/routes/routeMap';

export const catalogRoute: AppRoute = {
  key: 'catalog',
  path: '/catalog',
  title: 'Block Catalog',
  description: 'Catalog for reusable blocks and screen types',
};
